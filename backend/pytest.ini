[pytest]
testpaths = tests
asyncio_mode = auto
addopts = -q --strict-markers
filterwarnings =
    ignore::DeprecationWarning
    # LightGBM calls a scikit-learn validator whose keyword was renamed upstream.
    ignore:.*force_all_finite.*:FutureWarning
